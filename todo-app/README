# Updates the deployment and restarts the pod
kubectl apply -f manifests/deployment.yaml

# Provide the port, that we'd like access the server from
kubectl port-forward todo-app-dep-598ddbf954-52s8n 3003:3000
