apiVersion: v1
kind: Service
metadata:
  name: postgres-todo-svc
  namespace: project
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  type: ClusterIP
  # clusterIP: None
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
